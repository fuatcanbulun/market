import styled from "styled-components";

const NoResultBannerContainer = styled.div`
  height: 50px;
  width: 100%;
  line-height: 50px;
  text-align: center;
  font-size: 16px;
  font-family: marketFont400;
  color: var(--themeColor);
  border: 2px solid var(--themeColor);
`;

export { NoResultBannerContainer };
