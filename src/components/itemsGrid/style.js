import styled from "styled-components";

const ItemsGridContainer = styled.div`
  background: rgba(255, 255, 255, 0.5);
  backdrop-filter: blur(5px);
  border-radius: 15px;
  box-shadow: 0px 0px 3px 3px rgba(0, 0, 0, 0.1);
  display: flex;
  flex-flow: row wrap;
  padding: 8px;
`;

export { ItemsGridContainer };
